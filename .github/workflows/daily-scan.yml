name: Daily Scan
on:
  schedule:
    - cron: '0 9 * * *'
  workflow_dispatch:

jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - run: echo "test" > report.txt
      - uses: actions/upload-artifact@v4
        with:
          name: reports
          path: report.txt
